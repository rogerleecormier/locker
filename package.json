{
  "name": "locker-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "app",
    "cli"
  ],
  "overrides": {
    "kysely": "0.28.x"
  },
  "scripts": {
    "dev": "npm run dev -w app",
    "build": "npm run build -w app",
    "test": "npm run test --workspaces --if-present",
    "cli": "node cli/bin/locker.mjs"
  }
}
